<h1>Post一覧</h1>
<table>
  <tr>
    <th>内容</th>
  </tr>
<% @posts.each do |post| %>
  <tr>
    <td><%= post.content %></td>
    <td><%= link_to '投稿を確認する', post_path(post.id) %></td>
  </tr>
<% end %>
</table>
<%= link_to '新しくPostを投稿する', new_post_path %>